<UserControl x:Class="Patrimonio.Financas.Wpf.Cartoes.Views.CartaoFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:vm="clr-namespace:Patrimonio.Financas.Wpf.Cartoes.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:CartaoAlteracaoViewModel}">

    <Grid RowDefinitions="Auto, *, Auto">

        <!-- Formulário -->
        <Grid Grid.Row="0" RowDefinitions="Auto, Auto, Auto, *, Auto">

            <!-- Nome e Bandeira -->
            <Grid Grid.Row="0" ColumnDefinitions="*, *">

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Nome"/>
                    <TextBox Text="{Binding Nome}"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Bandeira"/>
                    <ComboBox ItemsSource="{Binding Source={x:Static vm:CartaoAlteracaoViewModel.Bandeiras}}"
                              SelectedItem="{Binding Bandeira}"/>
                </StackPanel>
            </Grid>

            <!-- Instituição e Limite -->
            <Grid Grid.Row="1" ColumnDefinitions="*, *">

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Instituição"/>
                    <ComboBox ItemsSource="{Binding Instituicoes}"
                              SelectedValue="{Binding InstituicaoId}"
                              SelectedValuePath="Id"
                              DisplayMemberPath="Nome"
                              IsEnabled="{Binding PodeAlterarInstituicao}"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Limite"/>
                    <TextBox Text="{Binding Limite}"/>
                </StackPanel>
            </Grid>


            <!-- Dia fechamento e dia vencimento -->
            <Grid Grid.Row="2" ColumnDefinitions="*, *">

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Dia fechamento"/>
                    <TextBox Text="{Binding DiaFechamento}"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Dia vencimento"/>
                    <TextBox Text="{Binding DiaVencimento}"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Espaço -->
        <Grid Grid.Row="1"/>

        <!-- Ações -->
        <StackPanel Grid.Row="2" Style="{StaticResource ActionStackPanelStyle}">
            <Button Content="Salvar" Command="{Binding SalvarCommand}"/>
            <Button Content="Cancelar" Command="{Binding CancelarCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
