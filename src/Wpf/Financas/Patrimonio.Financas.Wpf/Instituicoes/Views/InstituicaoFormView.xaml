<UserControl x:Class="Patrimonio.Financas.Wpf.Instituicoes.Views.InstituicaoFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:vm="clr-namespace:Patrimonio.Financas.Wpf.Instituicoes.ViewModels"
             mc:Ignorable="d"  
             d:DataContext="{d:DesignInstance vm:InstituicaoAlteracaoViewModel}">

    <Grid RowDefinitions="Auto, *, Auto">

        <!-- Formulário -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Nome"/>
            <TextBox Text="{Binding Nome}"/>
        </StackPanel>

        <!-- Espaço -->
        <Grid Grid.Row="1"/>

        <!-- Ações -->
        <StackPanel Grid.Row="2" Style="{StaticResource ActionStackPanelStyle}">
            <Button Content="Salvar" Command="{Binding SalvarCommand}"/>
            <Button Content="Cancelar" Command="{Binding CancelarCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
