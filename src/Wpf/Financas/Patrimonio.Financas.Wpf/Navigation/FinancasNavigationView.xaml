<UserControl x:Class="Patrimonio.Financas.Wpf.Navigation.FinancasNavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:nav="clr-namespace:Patrimonio.Financas.Wpf.Navigation"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance nav:FinancasNavigation}">
    
    <StackPanel>
        <Button Content="Categorias"
                Style="{StaticResource MenuButtonStyle}"
                Command="{Binding AbrirCategoriasCommand}"/>
        <Button Content="Cartões"
                Style="{StaticResource MenuButtonStyle}"
                Command="{Binding AbrirCartoesCommand}"/>
        <Button Content="Contas"
                Style="{StaticResource MenuButtonStyle}"
                Command="{Binding AbrirContasCommand}"/>
        <Button Content="Instituições"
                Style="{StaticResource MenuButtonStyle}"
                Command="{Binding AbrirInstituicoesCommand}"/>
        <Button Content="Movimentações"
                Style="{StaticResource MenuButtonStyle}"
                Command="{Binding AbrirMovimentacoesCommand}"/>
    </StackPanel>
</UserControl>
