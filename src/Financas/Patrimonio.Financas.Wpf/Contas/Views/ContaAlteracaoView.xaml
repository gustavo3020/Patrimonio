<UserControl x:Class="Patrimonio.Financas.Wpf.Contas.Views.ContaAlteracaoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:vm="clr-namespace:Patrimonio.Financas.Wpf.Contas.ViewModels"
             mc:Ignorable="d"  
             d:DataContext="{d:DesignInstance vm:ContaAlteracaoViewModel}">

    <Grid Margin="24" RowDefinitions="Auto,Auto,*,Auto">

        <!-- Cabeçalho -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Altere os dados da conta financeira." Foreground="Gray"/>
        </StackPanel>

        <!-- Formulário -->
        <Grid Grid.Row="1" RowDefinitions="Auto,Auto">

            <!-- Nome -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Nome" Margin="0,0,0,6"/>
                <TextBox Height="32" Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <!-- Instituição -->
            <StackPanel Grid.Row="1">
                <TextBlock Text="Instituição" Margin="0,0,0,6"/>
                <ComboBox Height="32" ItemsSource="{Binding Instituicoes}" SelectedValuePath="Id" SelectedValue="{Binding InstituicaoId}" DisplayMemberPath="Nome"/>
            </StackPanel>

        </Grid>

        <!-- Espaço -->
        <Grid Grid.Row="2"/>

        <!-- Ações -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar" Margin="0,0,8,0" Padding="16,8"/>
            <Button Content="Salvar" Padding="16,8" Command="{Binding SalvarCommand}"/>
        </StackPanel>

    </Grid>
</UserControl>