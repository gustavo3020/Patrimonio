<UserControl x:Class="Patrimonio.Financas.Wpf.Instituicoes.Views.InstituicaoListaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:vm="clr-namespace:Patrimonio.Financas.Wpf.Instituicoes.ViewModels"
             mc:Ignorable="d"  
             d:DataContext="{d:DesignInstance vm:InstituicaoListaViewModel}">

    <Grid Margin="24" RowDefinitions="Auto,*">

        <!-- Cabeçalho -->
        <Grid Grid.Row="0" Margin="0,0,0,20" ColumnDefinitions="*,Auto">
            <TextBlock Text="Instituições"
                       FontSize="20"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Criar"
                        Style="{StaticResource MenuButtonStyle}"
                        Command="{Binding AbrirCriacaoCommand}"/>
                <Button Content="Alterar"
                        Style="{StaticResource MenuButtonStyle}"
                        Command="{Binding AbrirAlteracaoCommand}"/>
                <Button Content="Excluir"
                        Style="{StaticResource MenuButtonStyle}"
                        Command="{Binding ExcluirCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Lista -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Instituicoes}"
                  SelectedItem="{Binding Selecionado}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Data de criação" Binding="{Binding DataCriacao, StringFormat='dd/MM/yyyy'}" Width="*"/>
                <DataGridTextColumn Header="Data de alteração" Binding="{Binding DataAlteracao, StringFormat='dd/MM/yyyy'}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
