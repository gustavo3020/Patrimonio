<UserControl x:Class="Patrimonio.Financas.Wpf.Contas.Views.ContaListaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:vm="clr-namespace:Patrimonio.Financas.Wpf.Contas.ViewModels"
             mc:Ignorable="d"  
             d:DataContext="{d:DesignInstance vm:ContaListaViewModel}">

    <Grid Margin="24" RowDefinitions="Auto,*">

        <!-- Cabeçalho -->
        <Grid Grid.Row="0" Margin="0,0,0,20" ColumnDefinitions="*,Auto">

            <StackPanel>
                <TextBlock Text="Contas" FontSize="28" FontWeight="SemiBold"/>
            </StackPanel>

            <Button Grid.Column="1" Content="+ Nova conta" Padding="16,8"/>

        </Grid>

        <!-- Lista -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Contas}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>

                <DataGridTextColumn Header="Instituição" Binding="{Binding InstituicaoNome}" Width="250"/>

                <DataGridTemplateColumn Header="Ações" Width="160">

                    <DataGridTemplateColumn.CellTemplate>

                        <DataTemplate>

                            <StackPanel Orientation="Horizontal">

                                <Button Content="Editar" Margin="0,0,8,0" Padding="10,4"/>
                                <Button Content="Excluir" Padding="10,4"/>

                            </StackPanel>

                        </DataTemplate>

                    </DataGridTemplateColumn.CellTemplate>

                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>