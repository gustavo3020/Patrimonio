<UserControl x:Class="Patrimonio.Financas.Wpf.Instituicoes.Views.InstituicaoAlteracaoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:vm="clr-namespace:Patrimonio.Financas.Wpf.Instituicoes.ViewModels"
             mc:Ignorable="d"  
             d:DataContext="{d:DesignInstance vm:InstituicaoAlteracaoViewModel}">

    <Grid Margin="24" RowDefinitions="Auto,*,Auto">

        <!-- Formulário -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Nome" Margin="0,0,0,6"/>
            <TextBox Text="{Binding Nome}" Height="32"/>
        </StackPanel>

        <!-- Espaço -->
        <Grid Grid.Row="1"/>

        <!-- Ações -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Margin="0,0,8,0" Padding="16,8"/>
            <Button Content="Salvar" Command="{Binding SalvarCommand}" Padding="16,8"/>
        </StackPanel>

    </Grid>

</UserControl>
