<UserControl x:Class="Patrimonio.Financas.Wpf.Instituicoes.Views.InstituicaoListaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:vm="clr-namespace:Patrimonio.Financas.Wpf.Instituicoes.ViewModels"
             mc:Ignorable="d"  
             d:DataContext="{d:DesignInstance vm:InstituicaoListaViewModel}">

    <Grid Margin="24" RowDefinitions="Auto,*">

        <!-- Cabeçalho -->
        <Grid Grid.Row="0" Margin="0,0,0,20" ColumnDefinitions="*,Auto">
            <StackPanel>
                <TextBlock Text="Instituições" FontSize="28" FontWeight="SemiBold"/>
            </StackPanel>
            <Button Grid.Column="1" Content="+ Nova instituição" Padding="16,8"/>
        </Grid>

        <!-- Lista -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Instituicoes}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  CanUserAddRows="False" 
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>

                <DataGridTemplateColumn Header="Ações" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Editar" Margin="0,0,8,0" Padding="10,4"/>
                                <Button Content="Excluir" Padding="10,4"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</UserControl>
